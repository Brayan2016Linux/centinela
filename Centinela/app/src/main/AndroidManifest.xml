<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imagineup.centinela" 
	android:versionCode="0"
	android:versionName="0.0.6">

    <!-- Permisos para la actividad principal-->
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.WRITE_SMS"/>

    <!--Aplicacion-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- Actividad de inicio -->
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Actividades de trabajo-->
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".ControlActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".BuyControlActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".BuyActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".EditBuy"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".PayControlActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".PaymentActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".EditPayment"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".CashAdvanceControlActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".CashAdvanceActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".EditCashAdvance"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".DialogActivity"
            android:theme="@style/AlertDialog.NewTheme"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".InfoCardActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".EditProfile"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".EditCard"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".AboutTo"
            android:theme="@style/AlertDialog.NewTheme"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ReviewCard"
            android:theme="@style/AlertDialog.CardReview"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:theme="@style/AlertDialog.NewTheme"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".BankFeesActivity"
            android:theme="@style/AlertDialog.NewTheme"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".HelpActivity"
            android:theme="@style/AlertDialog.NewTheme"
            android:label="@string/app_name">
        </activity>


    </application>

</manifest>
